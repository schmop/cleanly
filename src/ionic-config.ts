import { IonicConfig } from "@ionic/core";

export const ionicConfig: IonicConfig = {
    /**
     * Relevant Components: ion-alert, ion-infinite-scroll-content, ion-loading, ion-refresher-content, ion-toast. If true, content passed to the relevant components will be parsed as HTML instead of plaintext.
     */
    innerHTMLTemplatesEnabled: true,
}